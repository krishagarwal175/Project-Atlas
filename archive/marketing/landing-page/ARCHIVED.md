# ⚠️ ARCHIVED — not part of Atlas v1

This is a **marketing landing page** kept for a **possible future public release** (Phase 5+).

**It is not part of Atlas Version 1.** Atlas v1 is a local-first, single-user Strategic
Operating System — there is no public product, no waitlist, no signups, no hosting. See
[ADR-003](../../../vault/300-Architecture/Tech-Decisions/ADR-003-local-first-single-user.md).

Do not include this in the development workflow. It exists only so the work isn't lost if
Atlas is ever offered publicly. If that day comes, revive it deliberately via a new ADR.

`index.html` here is a self-contained static page (was previously deployed to Vercel as a
one-off; that deployment is not maintained and is unrelated to the local app).
